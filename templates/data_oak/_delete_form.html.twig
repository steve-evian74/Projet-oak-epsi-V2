<form method="post" action="{{ path('data_oak_delete', {'id': data_oak.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ data_oak.id) }}">
    <button class="btn">Delete</button>
</form>
